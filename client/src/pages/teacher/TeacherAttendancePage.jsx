import AttendancePage from '../admin/AttendancePage';

// Teachers use the same attendance page as admin
const TeacherAttendancePage = () => {
    return <AttendancePage />;
};

export default TeacherAttendancePage;