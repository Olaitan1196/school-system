import SettingsPage from '../admin/SettingsPage';

// Teachers use the same settings page as admin
const TeacherSettingsPage = () => {
    return <SettingsPage />;
};

export default TeacherSettingsPage;